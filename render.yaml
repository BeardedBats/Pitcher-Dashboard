services:
  - type: web
    name: pitcher-dashboard
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    healthCheckPath: /api/warmup-status
    envVars:
      - key: PORT
        value: 8000
